<ul>
  <% @manufacturers.each do |manufacturer| %>
    <li>
        <%= link_to manufacturer.name, manufacturer_path(manufacturer, show_archived: manufacturer.is_archived) %>
    </li>
  <% end %>
</ul>