<h1>All orders</h1>

<ul>
    <% @orders.each do |order| %>
        <li>
            <%= link_to "#{order.id}", order %>
        </li>
    <% end %>
</ul>

<%= link_to "Register new order", new_order_path %>